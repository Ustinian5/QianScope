/**
 * Legacy gateway alias retained so existing bookmarks and task links continue
 * to work after the product rename to QianScope.
 */
export { GET, POST } from '@/app/api/qianscope/[...path]/route';
